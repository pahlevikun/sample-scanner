abstract class PathIdentifier {
  static const String SPLASH = "/splash";
  static const String LOGIN = '/login';
  static const String HOME = '/home';
  static const String PRAYER_TIMES = '/prayer';
}

enum PageIdentifier {
  SPLASH,
  LOGIN,
  HOME,
  PRAYER_TIMES,
}

enum ProductIdentifier {
  SPLASH,
  AUTHENTICATION,
  MENU_HOLDER,
  PRAYER_TIMES,
}

enum BackdropWidgetIdentifier {
  PRAYER_TIMES,
}

enum SheetWidgetIdentifier {
  PRAYER_TIMES,
}
